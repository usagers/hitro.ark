export const isBoolean = (bool: Object | undefined | null) => {
  return bool === true || bool === false
}

export const isFalse = (bool: Object | undefined | null) => {
  return bool === false
}

export const isTrue = (bool: Object | undefined | null) => {
  return bool === true
}

